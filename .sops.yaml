# SOPS configuration: maps age public keys to encrypted secret files
# refer to secrets/README.md for detailed setup instructions
keys:
  - &user age1REPLACE_WITH_YOUR_AGE_PUBLIC_KEY

creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *user