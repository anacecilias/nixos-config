# Configuração do sops: define quais chaves age podem descriptografar
# quais arquivos. Veja secrets/README.md para o passo a passo completo.
keys:
  - &user age1SUBSTITUA_PELA_SUA_CHAVE_PUBLICA_AGE

creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *user
